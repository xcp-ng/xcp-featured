(jbuild_version 1)

(executables
 ((names (xcp_featured))
  (flags (:standard -safe-string))
  (libraries (systemd
              xcp.v6)
  )
 )
)

(install
 ((section bin)
  (files ((xcp_featured.exe as v6d)))
 )
)
